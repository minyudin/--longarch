# 启动命令（联调标准版）

> PowerShell 环境，默认工作目录：`D:\longarch1`

## 1) MQTT Broker（Mosquitto）

```powershell
Start-Process -FilePath "C:\Program Files\mosquitto\mosquitto.exe" -ArgumentList "-c","d:\longarch1\mosquitto-dev.conf","-v" -NoNewWindow
```

## 2) 后端（longarch-server）

```powershell
cd d:\longarch1\longarch-server
$env:JAVA_HOME='C:\Users\lenovo\.jdks\ms-17.0.16'
mvn spring-boot:run -DskipTests
```

## 3) 管理端（longarch-admin-next）

```powershell
cd d:\longarch1\longarch-admin-next
pnpm dev --host 127.0.0.1 --port 5173
```

## 4) 可视化大屏（bridge-viz，独立大屏前端 · 默认 5174）

```powershell
cd d:\longarch1\bridge-viz
pnpm install
pnpm dev
```

浏览器：`http://127.0.0.1:5174`（首次需在 **`/setup`** 粘贴管理端「大屏」页发放的 Token 或使用激活链接）。

## 5) miniapp H5（用于 MCP 浏览器联调）

```powershell
cd d:\longarch1\miniapp
pnpm build:h5
pnpm preview:h5
```

## 6) 传感器 MQTT 测试（可选）

```powershell
python d:\longarch1\test_mqtt_sensor.py
```

## 7) 执行设备 MQTT 模拟（可选）

```powershell
python d:\longarch1\tools\e2e\mqtt_actuator_sim.py --device-no ACT-XXXX --mqtt-host 127.0.0.1 --mqtt-port 1883 --mqtt-user longarch --mqtt-password longarch123 --keep-running
```

## 8) 内网穿透（如需公网硬件联调）

```powershell
D:\natapp_windows_amd64_3_0_3\natapp.exe -authtoken=7e3cc9a0c93bf0dd
```

## 9) 快速验活地址

- 管理端：`http://127.0.0.1:5173`
- 可视化大屏：`http://127.0.0.1:5174`
- miniapp H5：`http://127.0.0.1:12188`
- 后端健康检查（如已开放）：`http://127.0.0.1:8081`



cd d:\longarch1\miniapp
pnpm dev:weapp
